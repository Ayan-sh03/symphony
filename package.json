{
  "name": "symphony",
  "version": "1.0.0",
  "description": "Symphony: a long-running service that orchestrates Codex coding agents to work issues from a configured tracker.",
  "type": "module",
  "bin": {
    "symphony": "src/index.ts"
  },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.6.0"
  },
  "dependencies": {
    "liquidjs": "^10.16.1",
    "yaml": "^2.5.1"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.6.0"
  }
}
